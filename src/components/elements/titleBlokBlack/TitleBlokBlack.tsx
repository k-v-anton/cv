import React from 'react'
import styles from './TitleBlokBlack.module.scss'

export const TitleBlokBlack = ({title}: {title:string}) => {
  return (
    <h3 className={styles.title}>string</h3>
  )
}
